---
title: 拓扑排序算法
date: 2026-08-09 11:20:00
categories: 数据结构&算法
tags:
  - 图
  - 408
---

**拓扑排序**：由某个集合上的一个**偏序**得到该集合上的一个**全序**的操作。即有向图的拓扑排序——构造 AOV 网中顶点的一个拓扑线性序列，使得该线性序列满足两点：

1. **保持**原来有向图中顶点之间的优先关系
2. 对原图中**没有优先关系**的顶点之间，也建立一种（人为的）优先关系

## 基本概念

| 概念 | 说明 |
|:---|:---|
| **偏序** | 集合中仅有**部分**元素之间可以比较先后 |
| **全序** | 集合中**任意两个**元素之间都可以比较先后（拓扑排序后的线性序列） |
| **AOV 网** | 顶点表示活动，弧表示活动间的优先关系 |
| **前驱/后继** | 存在弧 $\langle i, j \rangle$，则 $i$ 是 $j$ 的直接前驱 |

**核心条件**：

| 条件 | 说明 |
|:---|:---|
| 图必须为 **DAG** | 有向无环图——若存在环，无法确定环上顶点的先后顺序 |
| 存在性 | DAG 的拓扑序列**一定存在**（至少一个） |
| 唯一性 | 拓扑序列通常**不唯一**（多入度为 0 时有多种选择） |

**AOV 网不能有环**：若存在环，则某项活动能否进行是以自身的完成作为前提条件，逻辑上不可能。检查方法——拓扑排序，**若所有顶点都在拓扑有序序列中，则无环**（否则存在环，拓扑排序失败）。

## 算法思想

![拓扑排序过程示意](/blog/images/第6章 图.assets/Gemini_Generated_Image_ylbi4uylbi4uylbi.png)

① 在 AOV 网中选择一个**没有前驱**的顶点（入度为 0）并**输出**；

② 在 AOV 网中**删除**该顶点以及从该顶点出发的所有有向弧（即邻接点入度 $-1$）；

③ **重复** ①②，直到：
- 图中**全部顶点都已输出** → 图中无环，拓扑排序成功
- 图中**不存在无前驱的顶点**（但仍有顶点未输出）→ 图中必有环，拓扑排序失败

## 算法实现

> 顶点结点需包含 `indegree` 字段。

**(1) 统计各顶点入度**：

```c++
void count_indegree(ALGraph *G) {
    int k;
    LinkNode *p;
    for (k = 0; k < G->vexnum; k++)                  // 顶点入度初始化
        G->AdjList[k].indegree = 0;

    for (k = 0; k < G->vexnum; k++) {                 // 顶点入度统计
        p = G->AdjList[k].firstarc;
        while (p != NULL) {
            G->AdjList[p->adjvex].indegree++;         // 邻接点入度 +1
            p = p->nextarc;
        }
    }
}
```

**(2) 拓扑排序算法**：

```c++
int TopologicSort(ALGraph *G, int topol[]) {         // topol[] 保存拓扑序列
    int k, no, vex_no, top = 0, count = 0, boolean = 1;
    int stack[MAX_VEX];                              // 用作堆栈
    LinkNode *p;

    count_indegree(G);                               // ① 统计各顶点的入度

    for (k = 0; k < G->vexnum; k++)                  // ② 入度为 0 的顶点入栈
        if (G->AdjList[k].indegree == 0)
            stack[++top] = G->AdjList[k].data;

    do {
        if (top == 0) boolean = 0;                   // 栈空 → 有环或无顶点可处理
        else {
            no = stack[top--];                       // 栈顶元素出栈
            topol[++count] = no;                     // 记录顶点序列

            p = G->AdjList[no].firstarc;
            while (p != NULL) {                      // ③ 删除以该顶点为尾的弧
                vex_no = p->adjvex;
                G->AdjList[vex_no].indegree--;       // 邻接点入度 -1
                if (G->AdjList[vex_no].indegree == 0)
                    stack[++top] = vex_no;           // 新的无前驱顶点入栈
                p = p->nextarc;
            }
        }
    } while (boolean == 0);

    if (count < G->vexnum) return -1;                // 有环，拓扑排序失败
    else return 1;                                    // 成功
}
```

## 算法分析

设 AOV 网有 $n$ 个顶点、$e$ 条边：

| 步骤 | 操作 | 时间复杂度 |
|:---|:---|:---|
| 统计各顶点入度 | 遍历所有顶点和边 | $O(n+e)$ |
| 入度为 0 的顶点入栈 | 遍历所有顶点 | $O(n)$ |
| 排序过程 | 顶点入栈/出栈 $n$ 次 + 入度减 1 共 $e$ 次 | $O(n+e)$ |
| **总计** | | **$O(n+e)$** |

空间复杂度：$O(n)$（栈 + 入度数组）。

## 交互演示

<div class="algo-demo">
  <iframe src="/blog/images/第6章 图.assets/topological-sort.html" title="拓扑排序" loading="lazy" allowfullscreen></iframe>
</div>

## 高频考点速记

| 考点 | 记忆 |
|:---|:---|
| 拓扑排序 | 反复输出入度 0 顶点并删弧（入度 $-1$），时间 $O(n+e)$ |
| 拓扑环检测 | 全部顶点输出→DAG 无环；仍有入度均 $>0$ 的顶点→有环 |
| 拓扑唯一性 | DAG 一定有拓扑序列；通常**不唯一**（多入度 0 时） |
| 应用场景 | 课程先修关系排课、工程任务调度、编译依赖分析 |
